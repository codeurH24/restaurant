{% extends 'base.html.twig' %}

{% block title %}Desire{% endblock %}

{% block body %}
    <h1>Desire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ desire.id }}</td>
            </tr>
            <tr>
                <th>Step</th>
                <td>{{ desire.step }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('desire_index') }}">back to list</a>

    <a href="{{ path('desire_edit', {'id': desire.id}) }}">edit</a>

    {{ include('desire/_delete_form.html.twig') }}
{% endblock %}
