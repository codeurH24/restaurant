{% extends 'base.html.twig' %}

{% block title %}ProductList{% endblock %}

{% block body %}
    <h1>ProductList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_list.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_list_index') }}">back to list</a>

    <a href="{{ path('product_list_edit', {'id': product_list.id}) }}">edit</a>

    {{ include('product_list/_delete_form.html.twig') }}
{% endblock %}
